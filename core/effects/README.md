# Core / Effects

GPU effects usable by any theme: `BlurEffect`, `GlowEffect`, `Particles`.

Must stay optional and degrade gracefully on low-end hardware — see the
performance rules in [`docs/Architecture.md`](../../docs/Architecture.md).

Empty until the architecture is validated.
