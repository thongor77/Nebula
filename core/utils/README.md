# Core / Utils

Non-visual helpers: `SoundManager`, `WallpaperEngine` (planned, see
[`docs/Roadmap.md`](../../docs/Roadmap.md)). `ThemeConfig` and `ThemeLoader`
ended up living in `core/config/` and `core/theme/` instead — see
[`docs/Core-Implementation-Status.md`](../../docs/Core-Implementation-Status.md).

Business logic lives here, not scattered inside QML components — see the
coding style rules in [`CONTRIBUTING.md`](../../CONTRIBUTING.md).

Currently unused.
