# Core / Services

Non-visual runtime services: `NebulaThemeLoader`, `NebulaAnimationManager`,
`NebulaSoundManager` (see [`docs/Core-API.md`](../../docs/Core-API.md)).

Empty as of Phase 1.1 — the foundation skeleton implements
`NebulaThemeConfig`/`NebulaThemeProvider` (in `core/config/` and
`core/theme/`) and `NebulaButton` first. See
[`docs/Core-Implementation-Status.md`](../../docs/Core-Implementation-Status.md).
