# Core / Assets

Shared fonts and icons used across every theme. Theme-specific assets
(wallpapers, custom icons) live inside the theme's own directory instead.

Currently unused — no shared Core-level font or icon has been needed yet;
each theme ships its own assets (see `themes/*/assets/`).
