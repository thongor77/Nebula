[General]
; Own identity, deliberately distinct from Nord (blue/sober) and Glass
; (frosted neutral) — a dark, teal-accented palette. Only Design System
; tokens already known to NebulaThemeConfig — see
; docs/Dashboard-Architecture-Stress-Test.md Gap 2: this file
; intentionally does NOT carry any Dashboard-specific key (showSystemInfo,
; leftPanel, ...) — those are plain QML properties in Main.qml instead,
; proving that path needs no Core/loader change.

backgroundColor=#10171a
surfaceColor=#1a2327
primaryColor=#3ddcc1
secondaryColor=#5a7a78
accentColor=#f2b134
textPrimary=#eaf4f2
textSecondary=#9db3b0
errorColor=#e2574c
successColor=#57c785
